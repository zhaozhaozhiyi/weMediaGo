name: Build Executables

on:
  push:
    tags:
      - 'v*'  # 推送版本标签时触发
  workflow_dispatch:  # 允许手动触发
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: false
        default: 'dev'

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web_app/package-lock.json

      - name: Install system dependencies (macOS)
        run: |
          brew install libpng jpeg libtiff

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build frontend
        working-directory: ./web_app
        run: |
          npm ci
          npm run build

      - name: Copy frontend files
        run: |
          cp -r web_app/dist/* iopaint/web_app/

      - name: Build executable with PyInstaller
        run: |
          pyinstaller wemediago.spec --clean --noconfirm

      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=$(echo ${{ github.ref }} | sed 's/refs\/tags\/v//')
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Package macOS app
        run: |
          cd dist
          VERSION="${{ steps.get_version.outputs.version }}"
          
          # Debug: List contents of dist directory
          echo "Contents of dist directory:"
          ls -la
          
          # macOS PyInstaller generates an executable file directory
          if [ -d "wemediago" ]; then
            # Package directory directly
            zip -r "wemediago-macos-${VERSION}.zip" wemediago/
            echo "Successfully packaged wemediago-macos-${VERSION}.zip"
          elif [ -f "wemediago" ]; then
            # If it's a single executable file
            mkdir -p wemediago-package
            cp wemediago wemediago-package/
            zip -r "wemediago-macos-${VERSION}.zip" wemediago-package/
            echo "Successfully packaged wemediago-macos-${VERSION}.zip"
          else
            echo "Error: Build artifact not found. Expected wemediago directory or wemediago file in dist folder."
            exit 1
          fi

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: wemediago-macos
          path: dist/wemediago-macos-*.zip
          retention-days: 30

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web_app/package-lock.json

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build frontend
        working-directory: ./web_app
        run: |
          npm ci
          npm run build

      - name: Copy frontend files
        shell: powershell
        run: |
          Copy-Item -Path "web_app\dist\*" -Destination "iopaint\web_app\" -Recurse -Force

      - name: Build executable with PyInstaller
        run: |
          pyinstaller wemediago.spec --clean --noconfirm

      - name: Get version
        id: get_version
        shell: powershell
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $VERSION = "${{ github.event.inputs.version }}"
          } else {
            $VERSION = "${{ github.ref }}".Replace("refs/tags/v", "")
          }
          Write-Output "version=$VERSION" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          Write-Output "Version: $VERSION"

      - name: Package Windows executable
        shell: powershell
        run: |
          cd dist
          $VERSION = "${{ steps.get_version.outputs.version }}"
          
          # Debug: List contents of dist directory
          Write-Host "Contents of dist directory:"
          Get-ChildItem -Recurse | Select-Object FullName, Mode
          
          # Check for build artifacts
          if (Test-Path "wemediago\wemediago.exe") {
            # Package directory containing .exe
            Compress-Archive -Path "wemediago\*" -DestinationPath "wemediago-windows-${VERSION}.zip" -Force
            Write-Host "Successfully packaged wemediago-windows-${VERSION}.zip"
          } elseif (Test-Path "wemediago.exe") {
            # Single .exe file
            Compress-Archive -Path "wemediago.exe" -DestinationPath "wemediago-windows-${VERSION}.zip" -Force
            Write-Host "Successfully packaged wemediago-windows-${VERSION}.zip"
          } elseif (Test-Path "wemediago") {
            # Package entire directory
            Compress-Archive -Path "wemediago" -DestinationPath "wemediago-windows-${VERSION}.zip" -Force
            Write-Host "Successfully packaged wemediago-windows-${VERSION}.zip"
          } else {
            Write-Error "Error: Build artifact not found. Expected wemediago.exe or wemediago directory in dist folder."
            exit 1
          }

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: wemediago-windows
          path: dist/wemediago-windows-*.zip
          retention-days: 30

  create-release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get version
        id: get_version
        run: |
          VERSION=$(echo ${{ github.ref }} | sed 's/refs\/tags\/v//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/wemediago-macos/*
            artifacts/wemediago-windows/*
          draft: false
          prerelease: false
          tag_name: ${{ github.ref_name }}
          name: Release ${{ steps.get_version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
